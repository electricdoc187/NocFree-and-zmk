/*
 * Copyright (c) 2026 The NocFree ZMK Contributors
 *
 * SPDX-License-Identifier: MIT
 */

#include <behaviors.dtsi>
#include <dt-bindings/zmk/bt.h>
#include <dt-bindings/zmk/keys.h>
#include <dt-bindings/zmk/outputs.h>

/*
 * Conservative default ANSI map.
 *
 * Bindings are listed in visual reading order. Each line pair is one physical
 * row: the left half first, then the right half.
 *
 * The retail ANSI keycaps carry Mac legends, so the bottom row reads
 * Fn / Control / Option / Command from the outside in. Option maps to ALT and
 * Command maps to GUI, which is the usual behaviour of a Mac keyboard on any
 * host. The bottom-row modifier order is the least certain part of this map and
 * should be confirmed against the physical legends; changing it is a keymap
 * edit only and does not affect the electrical map.
 */

/ {
    keymap {
        compatible = "zmk,keymap";

        default_layer {
            display-name = "Base";
            bindings = <
                &kp ESC   &kp F1    &kp F2    &kp F3    &kp F4    &kp F5    &kp F6
                &kp F7    &kp F8    &kp F9    &kp F10   &kp F11   &kp F12   &kp PSCRN &kp HOME

                &kp LANG5 &kp N1    &kp N2    &kp N3    &kp N4    &kp N5    &kp N6
                &kp N7    &kp N8    &kp N9    &kp N0    &kp MINUS &kp EQUAL &kp INT_YEN &kp BSPC

                &kp TAB   &kp Q     &kp W     &kp E     &kp R     &kp T
                &kp Y     &kp U     &kp I     &kp O     &kp P &kp LBKT &kp RBKT &kp DEL

                &kp CAPS  &kp A     &kp S     &kp D     &kp F     &kp G
                &kp H     &kp J     &kp K     &kp L &kp SEMI &kp SQT &kp BSLH &kp RET

                &kp LSHFT &kp Z     &kp X     &kp C     &kp V     &kp B
                &kp N     &kp M     &kp COMMA &kp DOT   &kp FSLH &kp INT_RO &kp RSHFT &kp UP

&kp LCTRL &kp LGUI       &kp LALT &lt 1 INT_MUHENKAN &kp SPACE
&kp SPACE &kp INT_HENKAN &kp RALT &mo 1             &kp RCTRL &kp LEFT &kp DOWN &kp RIGHT
            >;
        };

        /*
         * Function layer. Deliberately sparse: Bluetooth profile management,
         * output selection, and the two recovery behaviours.
         *
         *   Fn+1..Fn+5   select Bluetooth profile 0..4
         *   Fn+0         clear the selected Bluetooth profile
         *   Fn+U         send HID over USB
         *   Fn+B         send HID over Bluetooth
         *   Fn+F10/11/12 mute, volume down, volume up
         *   Fn+Esc       reset the left half
         *   Fn+Delete    reboot the right half into its bootloader
         *
         * ZMK reset behaviours act on the half whose key triggered them: Esc
         * is a left-half key and Delete a right-half one. Fn+Delete therefore
         * needs the split link up to reach the right half. Either half can
         * always be recovered over its USB CDC interface instead; see
         * docs/recovery.md.
         */
        function_layer {
            display-name = "Fn";
            bindings = <
                &sys_reset &trans     &trans     &trans     &trans     &trans     &trans
                &trans     &trans     &trans     &kp C_MUTE &kp C_VOL_DN &kp C_VOL_UP &trans &trans

                &trans     &bt BT_SEL 0 &bt BT_SEL 1 &bt BT_SEL 2 &bt BT_SEL 3 &bt BT_SEL 4 &trans
                &trans     &trans     &trans     &bt BT_CLR &trans     &trans     &trans     &trans

                &trans     &trans     &trans     &trans     &trans     &trans
                &trans     &out OUT_USB &trans   &trans     &trans     &trans     &trans     &trans

                &trans     &trans     &trans     &trans     &trans     &trans
                &trans     &trans     &trans     &trans     &trans     &trans     &trans  &bootloader

                &trans     &trans     &trans     &trans     &trans     &out OUT_BLE
                &trans     &trans     &trans     &trans     &trans     &trans     &trans     &trans

                &trans     &trans     &trans     &trans     &trans
                &trans     &trans     &trans     &trans     &trans     &trans     &trans    &trans
            >;
        };
    };
};
